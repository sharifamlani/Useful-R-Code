
#Counter
count <- 0
for(i in unique(Money.Network.2$ICPSR2)){
  for(j in unique(Money.Network.2$ICPSR2)){
    
    count<- count + 1
    
  }
}

count <- 0
for(i in unique(Money.Network.2$CMTE_ID)){
  for(j in unique(Money.Network.2$CMTE_ID)){
    
    count<- count + 1
    
  }
}